# PRP Journal — publication repository

This repository contains the approved, generated edition of the PRP Journal.
Editorial sources, research notes and review material live in the separate
`prp-editorial` repository.

The `public/` directory is generated. Do not edit it directly: replace it with
the output of `make release` from the editorial repository and review the Git
diff before committing.
